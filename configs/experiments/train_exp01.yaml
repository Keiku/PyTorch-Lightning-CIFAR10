# @package _group_

model:
  classifier: 'resnet18'
  implementation: 'scratch'

transform:
  normalization: 'cifar10'

dataset:
  root_dir: '/home/anasys/datasets'
  loading: 'custom'

train:
  batch_size: 512
  num_epochs: 200
